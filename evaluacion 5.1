#definir una funcion en python que acepte el readio y devuelva el valor del area de un circulo de esa dimensión

def calcular (r,pi):
    a = pi * r**2
    return (a)

r = float(input("dicte un numero  para el radio: "))
a = 3.1416*r**2

print("el resultado es igual  a: ",a)
